# SEO, legacy routes and hosting continuation

Local implementation only. No push, deployment, DNS change, CMS write, customer
submission, publication or credential read. This supersedes overlapping counts
and remaining-task statements in earlier 17 September checkpoints.

## Completed

- Eleven exact legacy redirects: ten topic category/tag URLs to their retained
  guides, plus the broken kitchen-unit-assembly link to kitchen fitting.
  No wildcard, media, admin or held-wardrobe redirect was introduced.
- Static `/thank-you` explains that visiting the page does not confirm receipt
  or a booking. It does not collect data and remains noindex.
- Held `/projects`, `/testimonials` and bedroom-furniture routes return 404;
  no fabricated project evidence, reviews or unconfirmed service was substituted.
  The owner-directed wardrobe article hold also remains. Original exports stay
  untouched; the accepted homepage's labelled portfolio preview is unchanged.
- Inert sitemap preparation selects 20 editorial/core URLs and excludes held
  and functional routes. Draft organisation data contains no ratings, address,
  qualifications or invented dates. Neither helper is imported by the app:
  no sitemap, structured data or indexing was published.

## Verification

- Both optimized offline builds pass, each generating 27 static pages.
- Local HTTP checker passes 1,451 assertions per mode (3100 sample, 3101
  candidate), including 11 HTTP 308 redirects, retained destinations, four held
  404s, safe thank-you, navigation, canonicals and noindex. These are assertions,
  not 2,902 distinct customer scenarios. No submissions are made by this checker.
- Numbered synthetic suites pass 1,200 checks, plus four offline CMS operations
  and the unnumbered preview check. Inventory structure passes with its existing
  123/124 media conflict and publication hold unchanged.
- Lint, standalone typecheck and tracked-diff whitespace checks pass.
- Both rebuilt homepages visually checked; meaningful content, key links and no
  framework overlay or captured warning/error. Sample and normal-mode banners
  remain distinct. Local review servers bind only to 127.0.0.1.
- The earlier mobile-check limitation was resolved using the supported browser
  CDP viewport control: Services verified at 375px with 375px document width,
  single-column cards and no horizontal overflow. Menu Enter/Escape and focus
  return passed. Pointer/touch activation was not established by that check.
  Temporary viewport settings were cleared.
- The historical source-content scanner remains a separate HOLD, not part of
  the green technical suite. It still flags broad business/service wording;
  no rule was weakened to manufacture a publication pass.

## Read-only hosting evidence

The Vercel connector returned project `prj_LQVeZOIeNzDGaH53EfXq3MTb2AJl`,
`kitchen3d-frontend`, framework Next.js, Node `24.x`. Its latest returned
deployment is `dpl_Ex52Rx4Ma1o4ybC4XBHZv4f7s4bw`, READY, target production,
from `DIGIAHUB/kitchen3d-frontend`, main, commit
`1d66d0f5b924c00d05d47acf449771eac5fb4743`.

This is the historical recovery commit, not the current local work. The returned
domain list contains three vercel.app aliases and no kitchen3d.co.uk domain.
This does not prove DNS state, deployment protection, runtime behaviour, secret
configuration or Git auto-deployment settings. No settings were changed and no
deployment logs, customer data or environment values were requested.

## Still required before launch

Complete outcomes for the remaining
legacy URL inventory (especially existing asset URLs); production-only removal
of internal preview notes; privacy notice and secure upload/retention handling;
actual durable admission/spam control and verified Wix bindings/receipts;
protected staging, credential-backed production build and end-to-end tests;
indexing activation only with release; backups, rollback and separately
authorised domain cutover. Internal dashboard/calendar work and new media
publication remain deferred as instructed. No background implementation is
scheduled by this checkpoint.

## Clean-install verification

An allowlisted copy of source/configuration, public review assets, scripts and
immutable data was made at
`C:\Users\omidm\AppData\Local\Temp\k3d-repro-dabab129116c4781a5ea963fc639e3b7`.
No `.env`, Git data, existing node_modules or build output was copied.
`npm ci --ignore-scripts --no-fund` installed 386 packages and audited 387,
reporting zero known vulnerabilities. Candidate build, lint and standalone
typecheck all passed from that clean installation. No server was started there.
The temporary evidence copy remains; the review repository was not reinstalled.

Package-lock SHA256 before and after:
`1A69FDA223773D03D45874C9F9A6163EE0FFA470D701F8A93695FA419B96AE58`.
This verifies clean-install compatibility, not byte-identical build output or
credential-backed production behaviour. The previously documented unsupported
ESLint 9 development-tool exception remains; no unsupported-tool-free claim is
made. No lockfile or dependency version changed during this continuation.

## Expanded legacy inventory check

`check-legacy-inventory.mjs` reads the original 309-row CSV without modifying it.
Its optional HTTP mode accepts only the two loopback origins, follows at most
three redirect steps within that origin, and performs no submissions. It checks
63 non-media URLs including their recorded slashes and query strings. All 576
assertions pass on each mode. Current observed outcomes:

| Outcome | Recorded URLs |
| --- | ---: |
| Retained response, including contact variants, robots and template-query homepage | 27 |
| Explicit relevant redirect | 11 |
| Held page/article, 404 | 4 |
| Legacy endpoints currently 404, disposition recorded below | 16 |
| Sitemap routes pending release preparation/activation, currently 404 | 5 |
| Media attachment/source-asset URLs, excluded from HTTP audit | 246 |

The 16 remaining legacy endpoint observations cover the uncategorized and two
wardrobe taxonomies, author archive, seven date archives, two feeds, WordPress
JSON endpoint, Elementor template endpoint and broken project-details URL.
No blanket home redirect or invented replacement content has been added. These
are current outcomes, not proof that every old URL is migrated or a blanket
retirement/publication approval. Existing source-asset preservation remains a
cutover requirement, separate from the deferred new portfolio publication.

The normal no-slash canonical form introduces Next.js slash normalization
before an explicit redirect for slash-suffixed legacy URLs. These chains are
bounded and tested; the earlier no-configured-chains assertion refers only to
the explicit redirect table, not to framework normalization.

## Next external boundary

A scoped approval popup requests protected, non-production staging on the
existing Vercel project using its existing server-side CMS read configuration.
No preview deployment is authorised by this document or performed yet.
Effective access protection must be verified first; robots/noindex alone are
not protection. DNS, production promotion, Forms/Contacts, real submissions,
bookings and additional media publication are excluded from that request.

SEO preparation follows Google's
[sitemap guidance](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap)
and [structured-data policies](https://developers.google.com/search/docs/appearance/structured-data/sd-policies):
canonical absolute URLs, no invented last-modified dates or unsupported claims.
